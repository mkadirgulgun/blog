@model PostModel


<article class="flex flex-col shadow my-4">
    <!-- Article Image -->


    @if(Model != null)
    {
        <img src="/uploads/@Model.Post.ImgUrl"  style="height:400px; width:800px"/>
        <div class="bg-white flex flex-col justify-start p-6">
            <h1 class="text-3xl font-bold pb-3">@Model.Post.Title</h1>
            @Html.Raw(Model.Post.Detail)
        </div>

        @Html.Partial("Partials/_Comment",Model)

    } else
    {
        <p>Üzgünüm böyle bir gönderi yok.</p>
    }

</article>